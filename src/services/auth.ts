import { LoginFormProps } from "../components/LoginForm/LoginForm";
import { mockUser } from "../mocks/user";

export const loginApi = (user: LoginFormProps) => {
  //authentcating
  user;
  return mockUser;
};
